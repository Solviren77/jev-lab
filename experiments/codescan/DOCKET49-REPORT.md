# docket-49 code-health report (8f9a15e1)

Pipeline: code parse → Jev (8 tuned questions, 743 calls, 10.5s, $0.0471) → Claude confirmation of the top 20 application functions plus every sensitive-data flag (37 functions). Read-only; nothing in docket-49 was changed.

| Severity / effort | Function | Confirmed issues | Proposed fix |
|---|---|---|---|
| high / large | `prototype/assistant.py::turn:326` | mixes_io_logic, logs_sensitive, more_than_name, hidden_side_effect, swallows_errors, should_split | Pull out build_shown_context(), resolve_navigation_controls(), a module-level guarded operation runner and a traced call_agent(), so turn() only orchestrates. Log or trace the swallowed ValueError at 349 and the control-JSON parse failure at 355. Document in the docstring that a turn can persist working records. |
| medium / small | `prototype/triage.py::main:162` | mixes_io_logic, logs_sensitive, broad_except, more_than_name, hidden_side_effect, should_split | Extract load_seen/save_seen and a report(got) printer, pass FIXED explicitly to classify instead of mutating a global, and narrow the except to RuntimeError/provider errors while logging the traceback. Consider storing hashes rather than 'shown' in the per-message JSON, or document the retention of that local directory. |
| medium / small | `experiments/navigation/live/pilot.py::finish:81` | mixes_io_logic, logs_sensitive, hardcoded_config, more_than_name, hidden_side_effect, should_split | Extract the phone-extraction and validation step and the transactional contact update into their own functions (for example extract_phone and apply_phone_update), and move the limits and prompt name to module constants. The repeated execute/fetchone calls hit different queries (contact check, phone read, update, audit insert), so none needs merging. |
| medium / small | `experiments/navigation/live/pilot.py::start:40` | mixes_io_logic, logs_sensitive, hardcoded_config, more_than_name, hidden_side_effect, should_split | Split this into validate_payload, reserve_request, query_jev and choose_target helpers, and make the thresholds and limits named constants. The second catalog() call at line 73 is a deliberate staleness recheck, not duplicate I/O. |
| medium / small | `prototype/mail_classify.py::propose:51` | mixes_io_logic, logs_sensitive, more_than_name, should_split | Split the logic into _classify(prompt, shown) for the model call and span, and a pure _decide(sender, answer, matters) for the matter and lane rules. Choose the D49_JEV_REVIEW route at the caller. |
| medium / small | `prototype/client_update.py::prepare:12` | mixes_io_logic, logs_sensitive, hardcoded_config, more_than_name, should_split | Move the model name and provider into module constants or provider.model() as matter_write does. Split the function into validate_event, build_message, call_model and validate_draft helpers. Drop the redundant try/except, since tracing.span already records failures. |
| medium / small | `prototype/matter_write.py::write_row:281` | mixes_io_logic, logs_sensitive, more_than_name, should_split | Move the D49_FIDELITY_EXPERIMENT dispatch to the caller or a thin wrapper. Extract the traced call-with-reask into ask_model(prompt, shown, row, issues) returning (answer, usage, refused), and keep write_row as the composition step. |
| medium / medium | `prototype/local_app.py::do_POST:2845` | mixes_io_logic, broad_except, hardcoded_config, more_than_name, should_split | Extract a single `_read_json(limit)` helper used by both the navigation branch and the main path (removing the duplicated length/rfile/json.loads code), and move the route dispatch into a table of (regex, handler) entries. Replace the navigation `except Exception` with specific exceptions and log unexpected ones. |
| medium / medium | `prototype/assistant.py::run:378` | mixes_io_logic, logs_sensitive, hardcoded_config, more_than_name, hidden_side_effect, should_split | Move the email-discipline rewrite into its own function (for example discipline_email(question, turns, body)), and move the target and open-record guards into a validate_operation() helper. Keep run as dispatch plus bookkeeping on an explicit state object instead of nonlocal closures. The repeated .update calls go to different dicts (directive, prepare, editing_usage), so they are not redundant I/O. |
| medium / medium | `prototype/assistant.py::operations_for:191` | mixes_io_logic, hardcoded_config, more_than_name, should_split | Replace the if-chain with a registry of small handler functions (calendar, register, work records, documents), and rename to something like build_operation_executor. Move 'America/Phoenix' and the register kinds to module constants. Also cache matters() inside pick, which calls list_matters up to twice per call (202-203); the two workspace_actions.execute calls themselves are distinct operations. |
| medium / medium | `prototype/tab_navigation.py::tabs:15` | mixes_io_logic, logs_sensitive, hardcoded_config, more_than_name, should_split | Split it into validate_payload, deterministic_route, build_criteria, ask_navigator and build_destination. Move the inventory path, prompt name and limits into module constants or config, and load the inventory once at import time. |
| medium / medium | `prototype/build/smoke_workspace_actions.py::run:39` | mixes_io_logic, hardcoded_config, more_than_name, hidden_side_effect, should_split | Split run into per-scenario functions (note_save, conflict, dictation, email_draft, template, disclosure) called from a viewport loop, and move a repeated animation-wait helper out of lines 118/135/141. Make OUTPUT and the viewports configurable. The repeated get_by_label, reload and write_text calls check different state after each step, so they are not redundant I/O. |
| medium / large | `prototype/local_app.py::do_GET:2436` | mixes_io_logic, broad_except, hardcoded_config, more_than_name, swallows_errors, should_split | Split into a route table dispatching to per-area handlers with a shared `_send_bytes(content_type, data, cache)` helper for the repeated header blocks. Narrow the email/cache except to Validation/sqlite errors and log anything else instead of returning an empty 200. |
| low / trivial | `prototype/guarded_generation.py::generate:9` | logs_sensitive, hardcoded_config | Move max_tokens=4000 into a named module constant or a parameter with a default. Serialise value once and reuse it in line 16 rather than calling json.dumps twice. |
| low / trivial | `tools/trace-digest/digest.py::render_span:229` | should_split | Optionally extract render_summary(attributes) and render_content(attributes, args) helpers so render_span only assembles sections and recurses. |
| low / trivial | `prototype/build/smoke_fuzzy_note.py::run:29` | mixes_io_logic, logs_sensitive, hardcoded_config, hidden_side_effect, should_split | Move the loop body into a per-viewport helper and make OUT configurable (an environment variable or tempfile). The two work.execute calls create different notes, so they are not redundant. |
| low / trivial | `prototype/matter_chat.py::ask:139` | mixes_io_logic, logs_sensitive | Acceptable as it is, since the tracing boundary enforces the content-free policy. Optionally move the span-attribute bookkeeping (lines 151-163) into a small helper so ask reads as validate, call and resolve. |
| low / trivial | `prototype/jev.py::ask:57` | mixes_io_logic, logs_sensitive | You could move the request-building and validation (58-64) into a small _request(prompt, state, questions) helper so ask() only orchestrates. Tracing content is already handled by the project-level policy. |
| low / trivial | `prototype/workspace_reads.py::_persist:42` | logs_sensitive | Set 0o600 on the temp file explicitly, or document that mkstemp already does, and consider storing only the fields the inbox needs rather than full message bodies. |
| low / trivial | `prototype/tracing.py::record_prompt:204` | logs_sensitive | No change is needed. Optionally note in the docstring that approved_by is personal data allowed only in the content project. |
| low / trivial | `prototype/matter_update.py::_call:46` | mixes_io_logic, logs_sensitive, hardcoded_config, more_than_name | Rename to something like _traced_model_call and pass the model and provider in as parameters instead of using the MODEL constant. The content capture is already gated by the tracing boundary, so no change is needed there. |
| low / small | `prototype/local_app.py::main:2940` | mixes_io_logic, broad_except, hardcoded_config, should_split | Extract `_google_clients(args)` that resolves client id/secret once and builds calendar and gmail; catch the specific workspace connection error instead of Exception. Also guard the finally block, which calls fcntl on service_lock even when it is None for non-serve paths (only reachable in serve today). |
| low / small | `prototype/local_app.py::page_records:1515` | mixes_io_logic, hardcoded_config, more_than_name, swallows_errors, should_split | Merge the two back-to-back `with connect(db_path)` blocks (1523 and 1531) into one, and move the href/date formatting into a small presentation helper. Keep the date fallback but consider logging unparseable dates. |
| low / small | `prototype/local_app.py::prepare_field_text:2097` | mixes_io_logic, more_than_name, should_split | Extract `_resolve_targets(payload, issues, rows)` and `_write_rows(...)` helpers; keep the two connections, which bracket the model calls. |
| low / small | `prototype/local_app.py::receive_document:1759` | mixes_io_logic, broad_except, hardcoded_config, swallows_errors, should_split | Move the 25 MB limit to a named constant, catch the PDF library's errors (or log the exception type) in the extraction fallback, and extract a `_store_received_document` helper. The re-select of artifact id after INSERT OR IGNORE (1790) is needed and the separate connections are legitimate. |
| low / small | `prototype/local_app.py::classify_google_mail:1393` | mixes_io_logic, more_than_name, hidden_side_effect, should_split | Extract `_ensure_mail_stored(db_path, account_id, local_id, fetch)` returning the message, so classify only proposes and persists. The three connections are separate transactions around a model call and should stay. |
| low / small | `prototype/local_app.py::draft_reply:2019` | mixes_io_logic, logs_sensitive, hardcoded_config, should_split | Move max_tokens to a named constant or prompt metadata, and extract `_reply_context(...)` to build `shown`. Consider storing only a subject hash or length in the audit row rather than the drafted subject. |
| low / small | `prototype/triage.py::classify:113` | mixes_io_logic, logs_sensitive, more_than_name, hidden_side_effect | Have the caller fetch the body and pass text in, move usage/answer attribute recording into a tracing helper, and drop the unused `schema` parameter. |
| low / small | `prototype/build/smoke_document_targeting.py::run:34` | mixes_io_logic, hardcoded_config, more_than_name, hidden_side_effect, should_split | Extract a check_viewport(page, width, live) helper and a cleanup step, lift timeouts/viewports/DOC id into named constants, and replace the repeated __import__('re')/__import__('local_app') calls with top-level imports. Repeated write_text/reload/get_by_label calls target different files or states and are legitimate. |
| low / small | `prototype/jev_templates.py::topics:11` | mixes_io_logic, hardcoded_config, more_than_name | Move the ALL_OTHER default and the review prompt name into module constants next to PROMPT. Optionally pull the definitions/questions construction into a helper and hoist the imports at L22-24 to module level. |
| low / small | `prototype/build/live_workspace_actions.py::ask:11` | mixes_io_logic, logs_sensitive, hardcoded_config, more_than_name, hidden_side_effect | Make the output path and matter id constants or CLI args, and write the results file once at the end of the script instead of on every ask(). Rename the function to ask_and_record, or move the recording and asserting into the caller. |
| low / small | `prototype/jev_workflow.py::prepare:179` | mixes_io_logic, hardcoded_config, more_than_name, should_split | Extract _claim(db_path, ...), _commit_result(...) and _mark_failed(...) helpers. Move the 60-minute lease into a named module constant. The three connect() calls are deliberately separate transactions around the long build(), so keep them. |
| low / small | `prototype/workspace_writes.py::local_drafts:161` | mixes_io_logic, should_split | Extract _read_drafts() and _write_drafts_atomic(drafts) helpers and keep local_drafts as validate-and-merge logic, with separate list and save entry points instead of the data=None switch. |
| low / medium | `prototype/local_app.py::prepare_checklist:1920` | mixes_io_logic, more_than_name, should_split | Split into `_checklist_inputs`, `_extract_checklist` (owning the D49_JEV_REVIEW/D49_FIDELITY_EXPERIMENT branch, read once via config) and `_store_checklist`. The two connections are legitimate (bracketing the model call) and should stay. |
| low / medium | `prototype/local_app.py::ask:1689` | mixes_io_logic, more_than_name, hidden_side_effect, should_split | Extract an `_assistant_request(db_path, request_id, digest)` context manager handling begin/replay/fail/complete, leaving `ask` to run the turn. Separate connections are distinct transactions and are fine. |
| low / medium | `prototype/build/smoke_persistent_intake.py::main:101` | mixes_io_logic, hardcoded_config, more_than_name, should_split | Split main into one function per scenario (navigation, review, layout sweep, tasks, drafts, restart, deferral, not-a-matter) that shares demo and page. Move the fixture dicts and page lists to module constants. The repeated Playwright calls are separate interactions and should stay. |
| none / trivial | `prototype/email_issues.py::_call:207` | logs_sensitive | No change needed. The content capture is governed by the tracing boundary; optionally add a comment pointing to docs/tracing.md. |
